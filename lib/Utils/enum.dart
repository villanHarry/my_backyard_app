enum Role { User, Business }

enum requestTypes { GET, POST, DELETE, PUT, PATCH }

enum socialType { phone, google, apple }

enum daysOfWeek {
  monday,
  tuesday,
  wednesday,
  thursday,
  friday,
  saturday,
  sunday
}

enum subscription_enums { user_sub, bus_sub_monthly, bus_sub_annually }
